{
	"name": "christophior.com",
	"version": "2.0.0",
	"description": "Christophior — an independent software company",
	"author": "Chris Villarreal",
	"scripts": {
		"build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -R static/* dist/",
		"predeploy": "npm run build",
		"deploy": "gh-pages -d dist"
	},
	"devDependencies": {
		"gh-pages": "^3.1.0"
	}
}
